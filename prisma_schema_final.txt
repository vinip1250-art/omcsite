
// COLE ISTO NO FINAL DO SEU prisma/schema.prisma:

// Contas (Miri, Vini, Lindy, etc)
model Account {
  id        String   @id @default(cuid())
  name      String
  type      String   @default("PERSON") // PERSON ou BUSINESS
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// Lojas/Clubes (Fast Shop, Casas Bahia, PagBank, etc)
model Store {
  id        String   @id @default(cuid())
  name      String   @unique
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  programs  PointsProgram[] // Uma loja pode ter vários programas
}

// E ATUALIZE o model PointsProgram para:
model PointsProgram {
  id        String   @id @default(cuid())
  name      String
  storeId   String?  // <-- ADICIONE ESTA LINHA
  store     Store?   @relation(fields: [storeId], references: [id]) // <-- ADICIONE ESTA LINHA
  type      String   @default("POINTS")
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  accounts PointsAccount[]
}
