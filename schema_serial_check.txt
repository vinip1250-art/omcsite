
// Verifique se o model Purchase no prisma/schema.prisma tem o campo:

model Purchase {
  id              String   @id @default(cuid())
  // ... outros campos ...
  serialNumber    String?  // <-- ADICIONE se não existir
  // ... resto dos campos ...
}

// Se não existir, adicione e rode:
// npx prisma migrate dev --name add_serial_number
// npx prisma generate
